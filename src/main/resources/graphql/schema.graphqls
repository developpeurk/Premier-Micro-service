scalar Date

type Query {
    accountsList: [BankAccount],
    accountById (id:String) : BankAccount
}

type BankAccount {
    id: String,
    createdAt: Date,
    balance: Float,
    currency: String,
    Type: String
}
